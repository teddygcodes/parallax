// TensionLayer — deck.gl HeatmapLayer stub
// Per spec: if deck.gl HeatmapLayer integration doesn't resolve cleanly
// within 2 iterations, export a no-op stub. Using stub to avoid blocking
// other features and build.
export default function TensionLayer() {
  return null
}
